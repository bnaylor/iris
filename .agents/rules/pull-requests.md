---
trigger: always_on
description: Always open Pull Requests for changes in the Iris project
---

# Iris Project Pull Request Rule

- **Always Open Pull Requests**: For any changes in the Iris project (`~/src/iris`), always create a branch and open a Pull Request rather than merging directly into `main`.
